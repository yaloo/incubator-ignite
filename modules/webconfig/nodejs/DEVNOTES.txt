Ignite Web Configurator Build Instructions
======================================

Before run need install locally:

1. NodeJS(npm)
2. MongoDB

To run:

1. Run following command in terminal for upload all dependence: "npm install" in app folder.
2. Run MongoDB.
3. Start application by following command in terminal: "npm start" in app folder.

Open in browser: localhost:3000